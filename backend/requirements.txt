# ============================================================
# NexusAI 后端依赖
# Python 版本: 3.10+
# ============================================================

# ---------- Web 框架 ----------
fastapi==0.115.12               # 0.115.0 锁死了 starlette<0.39，与 mcp SDK 冲突；升级到 0.115.12 解除
uvicorn[standard]==0.30.6
python-multipart==0.0.10          # 表单/文件上传支持

# ---------- 数据校验与配置 ----------
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# ---------- 数据库 ----------
sqlalchemy==2.0.35
psycopg2-binary==2.9.9            # PostgreSQL 驱动（同步）
alembic==1.13.3                   # 数据库迁移

# ---------- 认证与安全 ----------
python-jose[cryptography]==3.3.0  # JWT 令牌签发/校验
passlib[bcrypt]==1.7.4            # 密码哈希
bcrypt==4.0.1                     # passlib 兼容版本，避免警告

# ---------- 异步任务队列 ----------
celery==5.4.0
redis==5.1.0

# ---------- 日志与工具 ----------
loguru==0.7.2                     # 美观的日志库

# ---------- 阶段二：RAG 管道 ----------
chromadb-client==0.5.23           # ChromaDB HTTP 客户端（避开 Windows 本地 C++ 编译，连接 docker 运行的 chroma server）
langchain-text-splitters==0.3.2   # 仅引入分块策略，不引入整个 langchain
dashscope==1.20.11                # 阿里通义 Embedding / LLM SDK
unstructured>=0.15.0              # 文档结构化解析（自动检测类型、提取标题/表格/列表等）
jieba==0.42.1                     # 中文分词（BM25 关键词检索用）

# ---------- 阶段三：Agent 引擎 ----------
langgraph==0.2.34                 # Agent 状态机框架（核心）
openai==1.57.0                    # 1.54 与 httpx 0.28+ 不兼容（proxies 参数已移除），1.57 修复
tiktoken==0.8.0                   # Token 计算（摘要压缩判断阈值用）
mcp>=1.2.0                        # Anthropic 官方 MCP SDK（双向集成）
tavily-python>=0.5.0              # Tavily Search API（AI 专用搜索，中文效果好）

# ---------- 阶段五：企业化加固 ----------
slowapi==0.1.9                    # FastAPI 限流中间件（防 LLM 滥用）
