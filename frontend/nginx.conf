# NexusAI 前端 nginx 配置
# - 静态文件托管 + SPA history 路由 fallback
# - /api 反向代理到后端服务（容器内通过服务名 backend 解析）
# - SSE 关键：禁用代理缓冲

server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # 上传文件大小限制（知识库文档上传）
    client_max_body_size 50m;

    # gzip 压缩静态资源
    gzip on;
    gzip_types text/plain text/css application/javascript application/json image/svg+xml;
    gzip_min_length 1024;

    # ---------- API 反向代理 ----------
    location /api/ {
        # 动态 DNS 解析：使用容器实际的 DNS 解析器（启动时从 /etc/resolv.conf 自动提取）
        # 每 5 秒刷新一次，解决后端重部署 IP 漂移导致 Nginx 缓存旧 IP 超时的问题
        resolver ${DNS_RESOLVER} valid=5s;

        # 通过变量承载后端地址，迫使 Nginx 配合 resolver 动态解析（而非启动时一次性缓存）
        set $backend "${BACKEND_URL}";
        proxy_pass $backend;

        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # SSE 必须：关闭缓冲、保持连接、不超时
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 600s;
        proxy_send_timeout 600s;
        chunked_transfer_encoding on;
    }

    # ---------- SPA 路由 fallback ----------
    location / {
        try_files $uri $uri/ /index.html;
    }

    # ---------- 静态资源缓存 ----------
    location ~* \.(?:js|css|woff2?|ttf|otf|eot|svg|png|jpg|jpeg|gif|ico|webp)$ {
        expires 7d;
        add_header Cache-Control "public, immutable";
    }
}
